---
phase: 01-ecommerce-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - portfolio/src/app/(projects)/projects/e-commerce/page.tsx
autonomous: true
requirements: [FIX-ECOMMERCE]
must_haves:
  truths:
    - "Meeting images are positioned below the pitch research approach section"
    - "Inline font styles with invalid weights are removed"
    - "Full width images maintain a 1.9 aspect ratio"
    - "Only Reckless Neue and Alegreya Sans fonts are used"
  artifacts:
    - path: portfolio/src/app/(projects)/projects/e-commerce/page.tsx
      provides: "Updated E-commerce project page"
  key_links: []
---

<objective>
Implement the requested fixes for the E-commerce case study page.

Purpose: Improve visual consistency, correct layout sequence, and enforce the project's typography system.
Output: Updated `page.tsx` for the E-commerce project.
</objective>

<execution_context>
@/Users/pinata/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/pinata/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@portfolio/src/app/(projects)/projects/e-commerce/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reorder Meeting Images</name>
  <files>portfolio/src/app/(projects)/projects/e-commerce/page.tsx</files>
  <action>
    Find the two full-width images labeled "Pitch Research Flow" and "Pitch Synthesis" (src: `0YQtFNvW...` and `3a9847NI...`).
    Move their `<Image>` blocks so they are positioned immediately BELOW the `<div id="research" ...>` section (Pitch Research Approach), rather than above it.
  </action>
  <verify>Check that the images appear after the "Pitch Research Approach" section in the code.</verify>
  <done>Meeting images are correctly sequenced below the research approach content.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Typography Styles</name>
  <files>portfolio/src/app/(projects)/projects/e-commerce/page.tsx</files>
  <action>
    Remove all instances of inline `style={{fontWeight: "350"}}`, `fontWeight: "300"`, `fontWeight: "500"`, and `fontFamily` from the elements in `page.tsx`.
    Replace them with appropriate Tailwind classes (e.g., `font-light` for 300, `font-normal` for 400, `font-medium` for 500).
    Ensure that no arbitrary font names are used and all text implicitly relies on the global CSS fonts (Reckless Neue for headings, Alegreya Sans for body) or explicit Tailwind font classes (`font-recklessneue`, `font-alegreya-sans`).
  </action>
  <verify>Grep for `fontWeight` in the file and ensure there are no hardcoded invalid weights like `350`.</verify>
  <done>All inline font styles are removed and replaced with valid Tailwind classes.</done>
</task>

<task type="auto">
  <name>Task 3: Apply 1.9 Aspect Ratio to Full Width Images</name>
  <files>portfolio/src/app/(projects)/projects/e-commerce/page.tsx</files>
  <action>
    Locate all full-width `<Image>` components (those with `className="w-full ..."` that span the viewport).
    Add the Tailwind classes `aspect-[1.9/1] object-cover` to their `className` strings to enforce the requested 1.9 width/aspect ratio.
    Maintain their `sizes="100vw"` attribute.
  </action>
  <verify>Check that full width images contain `aspect-[1.9/1]` and `object-cover`.</verify>
  <done>Full width images scale correctly to a 1.9 aspect ratio.</done>
</task>

</tasks>

<verification>
Ensure all requested fixes are present in the code without breaking existing layouts or causing hydration errors.
</verification>

<success_criteria>
- Meeting images are moved below the research approach section.
- Inline `fontWeight` styles are replaced with Tailwind classes.
- Full width images have `aspect-[1.9/1] object-cover`.
- Only Reckless Neue and Alegreya Sans fonts are used.
</success_criteria>

<output>
After completion, create `.planning/phases/01-ecommerce-fixes/01-01-SUMMARY.md`
</output>
